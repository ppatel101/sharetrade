<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <% if user_signed_in? %>
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item">
        <%= link_to 'Home', root_path, class: "nav-link active" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Leaves', leave_applications_path, class: "nav-link active" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Updates', updates_path ,class: "nav-link active" %>
      </li>
      <li class="nav-item">
        <%= link_to unread_notification_count > 0 ? "Notifications <span class='badge badge-light'>#{unread_notification_count}</span>".html_safe : "Notification",
        notification_index_path,
        class: unread_notification_count > 0 ? "btn btn-primary btn-sm mt-1" : "nav-link active" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Systems', systems_path ,class: "nav-link active" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Projects', projects_path ,class: "nav-link active" %>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Account
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <span class="dropdown-item"> 
            <%= current_user.name%>
          </span>
          <span class="dropdown-item">
            <%= current_user.email%>
          </span>
          <div class="dropdown-divider"></div>
            <%= link_to 'Your Profile', root_path, class: "dropdown-item text-primary" %>
            <%= link_to 'Notifications', notification_index_path ,class: "dropdown-item text-primary" %>
            <%= link_to 'Change Password', passwords_edit_path, class: "dropdown-item text-primary" %>
          <div class="dropdown-divider"></div>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item text-primary"%>
        </div>
      </li>
    </ul>
  <% else %>
    <div class="collapse navbar-collapse" id="navbarNavDropdown"> 
      <span class="mr-auto" style="color:white">Employee Portal</span>
      <ul class="navbar-nav ml-auto mr-4">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Login
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to 'Admin', new_admin_session_path, class: "dropdown-item" %>
            <%= link_to 'Employee', new_user_session_path, class: "dropdown-item" %>
          </div>
        </li>
      </ul>
    </div>
  <% end %>
</nav>